let num = prompt("Enter a number: ");
let total = 0;
while( num != -1 ) {
    num = Number(prompt("Enter a number: "));
    alert(num);
    total += num;
    alert(total)
   }

let i = 1;

while (i < 100) {

    document.write("<hr width = " + i + "%>");
    i++;
}

